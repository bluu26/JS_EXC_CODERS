// const printTable = (array) => {
//     foreach(element in array)
//         console.log(element)
// }
function printTable(array) {
    array.forEach((element) => {
        console.log(element)
    })
}